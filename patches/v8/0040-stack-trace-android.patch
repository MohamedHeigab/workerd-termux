diff --git a/include/v8config.h b/include/v8config.h
index b36169f..99d8dd0 100644
--- a/include/v8config.h
+++ b/include/v8config.h
@@ -22,6 +22,46 @@ path. Add it with -I<path> to the command line
 #endif
 
 #include <memory>
+#include <atomic>
+
+#if !defined(__cpp_lib_atomic_ref) && !defined(_ATOMIC_REF_SHIM_DEFINED)
+#define _ATOMIC_REF_SHIM_DEFINED 1
+namespace std {
+template <typename T>
+struct atomic_ref {
+  static constexpr size_t required_alignment = alignof(T);
+  static constexpr bool is_always_lock_free = true;
+
+  explicit atomic_ref(T& obj) noexcept : ptr_(&obj) {}
+  atomic_ref(const atomic_ref&) noexcept = default;
+  atomic_ref& operator=(const atomic_ref&) = delete;
+
+  void store(T desired, std::memory_order order = std::memory_order_seq_cst) const noexcept {
+    __atomic_store_n(ptr_, desired, static_cast<int>(order));
+  }
+  T load(std::memory_order order = std::memory_order_seq_cst) const noexcept {
+    return __atomic_load_n(ptr_, static_cast<int>(order));
+  }
+  T exchange(T desired, std::memory_order order = std::memory_order_seq_cst) const noexcept {
+    return __atomic_exchange_n(ptr_, desired, static_cast<int>(order));
+  }
+  bool compare_exchange_strong(T& expected, T desired, std::memory_order success, std::memory_order failure) const noexcept {
+    return __atomic_compare_exchange_n(ptr_, &expected, desired, false, static_cast<int>(success), static_cast<int>(failure));
+  }
+  bool compare_exchange_strong(T& expected, T desired, std::memory_order order = std::memory_order_seq_cst) const noexcept {
+    return __atomic_compare_exchange_n(ptr_, &expected, desired, false, static_cast<int>(order), static_cast<int>(order));
+  }
+  T fetch_add(T arg, std::memory_order order = std::memory_order_seq_cst) const noexcept {
+    return __atomic_fetch_add(ptr_, arg, static_cast<int>(order));
+  }
+  T fetch_or(T arg, std::memory_order order = std::memory_order_seq_cst) const noexcept {
+    return __atomic_fetch_or(ptr_, arg, static_cast<int>(order));
+  }
+ private:
+  T* ptr_;
+};
+}  // namespace std
+#endif
 // clang-format off
 
 // Platform headers for feature detection below.
diff --git a/src/base/debug/stack_trace_android.cc b/src/base/debug/stack_trace_android.cc
index 16fbf98..00334f1 100644
--- a/src/base/debug/stack_trace_android.cc
+++ b/src/base/debug/stack_trace_android.cc
@@ -12,7 +12,7 @@
 #include <string.h>
 #include <unwind.h>
 
-#include <src/base/platform/platform.h>
+#include "src/base/platform/platform.h"
 
 #include <iomanip>
 #include <ostream>
diff --git a/src/compiler/turboshaft/wasm-shuffle-reducer.cc b/src/compiler/turboshaft/wasm-shuffle-reducer.cc
index 04fa143..3c4d522 100644
--- a/src/compiler/turboshaft/wasm-shuffle-reducer.cc
+++ b/src/compiler/turboshaft/wasm-shuffle-reducer.cc
@@ -519,7 +519,7 @@ void WasmShuffleAnalyzer::ProcessShuffleOfLoads(const Simd128ShuffleOp& shuffle,
 
   if (GetDemandedBytes(&shuffle).IsAll()) {
     // Full width shuffles.
-    SimdShuffle::ShuffleArray shuffle_bytes;
+    SimdShuffle::ShuffleArray<kSimd128Size> shuffle_bytes;
     std::copy_n(shuffle.shuffle, kSimd128Size, shuffle_bytes.begin());
     auto canonical = SimdShuffle::TryMatchCanonical(shuffle_bytes);
     switch (canonical) {
